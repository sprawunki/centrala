<?php

$finder = PhpCsFixer\Finder::create()
    ->in(__DIR__)
    ->append([__DIR__.'/app']);

$config = PhpCsFixer\Config::create()
    ->setRiskyAllowed(true)
    ->setRules([
        '@PSR2' => true,
    ])
    ->setFinder($finder);

return $config;
