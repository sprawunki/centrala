parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcsfixer2:
            config: .php_cs.dist
        phpcs:
            standard: phpcs.ruleset.xml
        phpstan:
            level: 7
        phpunit: ~
        phpspec: ~
